package ir.bank.qh.common.enums;

/** Organizational level required to approve/override a rule (e.g. {@code APPROVAL_LEVEL_CODE}, {@code OVERRIDE_APPROVAL_LEVEL_CODE}). */
public enum ApprovalLevel {
    BRANCH_MANAGER,
    REGIONAL_MANAGER,
    COMMITTEE,
    HEAD_OFFICE
}
