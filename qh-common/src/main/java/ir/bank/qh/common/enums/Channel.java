package ir.bank.qh.common.enums;

/** Delivery/service channel (e.g. {@code CHANNEL_CODE}). {@code ALL} is a wildcard meaning every channel. */
public enum Channel {
    ALL,
    BRANCH,
    MOBILE_BANK,
    INTERNET_BANK
}
