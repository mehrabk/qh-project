package ir.bank.qh.party.enums;

/** {@code CONTEXT_TYPE_CODE} on PARTY_ROLE - which business object the role applies to. */
public enum ContextType {
    ACCOUNT,
    CONTRACT,
    FACILITY,
    CASE,
    GROUP,
    PRODUCT
}
