package ir.bank.qh.party.enums;

/** {@code SIGNING_RULE_CODE} on SIGNATURE_SPECIMEN - required signing combination for a mandate. */
public enum SigningRule {
    SINGLE,
    JOINT,
    ANY_TWO,
    ALL_REQUIRED
}
