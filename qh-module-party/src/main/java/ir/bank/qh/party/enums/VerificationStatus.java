package ir.bank.qh.party.enums;

/** Used wherever the diagram carries a {@code *_VERIFICATION_STATUS_CODE} column. */
public enum VerificationStatus {
    UNVERIFIED,
    PENDING,
    VERIFIED,
    REJECTED,
    EXPIRED
}
