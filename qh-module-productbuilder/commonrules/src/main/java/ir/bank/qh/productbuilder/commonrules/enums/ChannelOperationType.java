package ir.bank.qh.productbuilder.commonrules.enums;

/** {@code OPERATION_CODE} on ProductChannelOperation - which lifecycle operation a channel allows. */
public enum ChannelOperationType {
    OPEN,
    APPLY,
    SERVICE,
    CLOSE
}
