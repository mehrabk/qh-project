package ir.bank.qh.productbuilder.commonrules.enums;

/** {@code GENDER_CODE} on ProductEligibilityRule. {@code ANY} means the rule doesn't restrict by gender. */
public enum Gender {
    MALE,
    FEMALE,
    ANY
}
