package ir.bank.qh.productbuilder.commonrules.enums;

/** {@code REQUIREMENT_STAGE_CODE} on ProductRequiredDocument / ProductRequiredInquiry. */
public enum RequirementStage {
    OPENING,
    APPLICATION
}
