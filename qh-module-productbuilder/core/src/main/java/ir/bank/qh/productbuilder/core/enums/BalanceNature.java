package ir.bank.qh.productbuilder.core.enums;

/** {@code BALANCE_NATURE_CODE} on Product - CK_PRODUCT_CLASS_NATURE ties this to productClass (DEPOSIT -> LIABILITY, LOAN -> ASSET). */
public enum BalanceNature {
    ASSET,
    LIABILITY
}
