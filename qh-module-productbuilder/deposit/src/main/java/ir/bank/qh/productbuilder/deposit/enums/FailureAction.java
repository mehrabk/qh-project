package ir.bank.qh.productbuilder.deposit.enums;

/** {@code FAILURE_ACTION_CODE} on DepositProductClosurePrecheck - what happens when the precheck fails. */
public enum FailureAction {
    BLOCK,
    WARN
}
