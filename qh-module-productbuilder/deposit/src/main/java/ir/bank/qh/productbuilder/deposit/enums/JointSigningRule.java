package ir.bank.qh.productbuilder.deposit.enums;

/** {@code SIGNING_RULE_CODE} on DepositProductJointRule - how many joint owners must sign to authorize a transaction. */
public enum JointSigningRule {
    ANY_ONE,
    ALL_REQUIRED
}
