package ir.bank.qh.productbuilder.loan.enums;

/** {@code GRACE_METHOD_CODE} on LoanProductRepaymentRule - how interest accrued during the grace period is handled. */
public enum GraceMethod {
    DEFERRED,
    CAPITALIZED
}
