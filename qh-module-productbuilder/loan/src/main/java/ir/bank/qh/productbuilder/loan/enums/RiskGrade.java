package ir.bank.qh.productbuilder.loan.enums;

/** {@code MAX_RISK_GRADE_CODE} on LoanEligibilityExtension - a letter-grade credit-risk scale, best to worst. */
public enum RiskGrade {
    A,
    B,
    C,
    D
}
