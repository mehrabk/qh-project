package ir.bank.qh.productbuilder.reference.enums;

/** {@code OPERATION_TYPE_CODE} on Operation - classifies an accounting operation (whose own business key is operationCode). */
public enum OperationType {
    CREDIT,
    DEBIT
}
